{
  "name": "dev",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "bootstrap": "lerna bootstrap",
    "graph": "nx graph",
    "lint": "run-s lint:*",
    "lint:prettier": "prettier --check .",
    "lint:eslint": "eslint .",
    "lint-files": "run-s 'lint-files:* -- {*}' --",
    "lint-files:prettier": "prettier --check --no-error-on-unmatched-pattern",
    "lint-files:eslint": "eslint --no-error-on-unmatched-pattern",
    "test": "lerna run test --since=origin/master"
  },
  "devDependencies": {
    "@janejeon/eslint-config": "*",
    "@janejeon/prettier-config": "*",
    "husky": "^8.0.1",
    "lerna": "^6.0.1",
    "lint-staged": "^13.0.3",
    "npm-run-all": "^4.1.5",
    "skip-ci": "*"
  }
}
