{
  "name": "dev",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "bootstrap": "lerna bootstrap --hoist"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lerna": "^6.0.1",
    "lint-staged": "^13.0.3",
    "skip-ci": "^1.0.4"
  }
}
